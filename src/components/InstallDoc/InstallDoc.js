import { withStyles } from 'packages'
import { props, styles as buttonStyles } from 'packages/Button/Button'
import UsageDoc from '!!raw-loader!./Usage.doc.js'
import sortBy from 'lodash/sortBy'
import Doc from '../Doc'

const styles = theme => {
  return {
    
  }
}

const InstallDoc = {
  render () {

  }
}

export default withStyles(styles)(InstallDoc)